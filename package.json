{
  "name": "kairos-clock-bot",
  "version": "1.0.0",
  "description": "Automated Kairos clock-in and clock-out workflow.",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dry-run:clock-in": "tsx src/index.ts --execution-mode local --dry-run --action clock-in",
    "dry-run:clock-out": "tsx src/index.ts --execution-mode local --dry-run --action clock-out",
    "punch": "tsx src/index.ts",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "luxon": "^3.7.2",
    "playwright": "^1.59.1"
  },
  "devDependencies": {
    "@types/luxon": "^3.7.1",
    "@types/node": "^25.5.2",
    "tsx": "^4.21.0",
    "typescript": "^6.0.2",
    "vitest": "^4.1.3"
  }
}
